import { page } from "@roxi/routify";
import { derived } from "svelte/store";

// function statsStore() {
//   derived(page, )
// }

// export const stats = new Stats();

// Create `track` method in a derived store so it's using the correct page